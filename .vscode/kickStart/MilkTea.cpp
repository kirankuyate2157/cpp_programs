#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t = 1, T;
    cin >> T;
    while (t <= T)
    {
        int n, p, m;
        cin >> n >> p >> m;

        t++;
    }
    return 0;
}