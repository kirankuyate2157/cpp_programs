#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a = 0, b = 0, c = 10, d = 20;
    double ans = (a / (double)2);
    cout << (a / 2) << " ";
    ans += (double)b;
    ans += (double)2 * c;
    ans += (double)5 * d;
    cout << ans << endl;

    return 0;
}